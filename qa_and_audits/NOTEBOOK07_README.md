
# Notebook 07 — External Multi-Sensor Gating Benchmark

File: `07_real_external_multisensor_gating.ipynb`

Dataset: **UCI Daily and Sports Activities**  
DOI: **10.24432/C5C59F**  
Official page: https://archive.ics.uci.edu/dataset/256/daily

## Run modes

### Normal
Run all cells. The notebook downloads the official 162.9 MB UCI archive on first use.

### Full publication run
Before running all cells:

```python
import os
os.environ["FULL_RUN"] = "1"
```

### Smoke verification
```python
import os
os.environ["SMOKE_TEST"] = "1"
```

Smoke mode uses a deterministic schema-matched surrogate and is **only for code verification**.
It must never be reported as external empirical evidence.

## Outputs
The final cell produces:
`notebook07_external_results.zip`

It contains every CSV, JSON, and figure generated by the notebook.
